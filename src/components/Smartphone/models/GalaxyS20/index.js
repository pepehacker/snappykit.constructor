import React from 'react';

// Styles
import Flat, { GALAXY_S20_FLAT_COLOR } from './Flat';

const SmartphoneGalaxyS20 = (props) => <Flat {...props} />;

export default SmartphoneGalaxyS20;

export const GALAXY_S20_COLOR = {
  FLAT: GALAXY_S20_FLAT_COLOR
};
